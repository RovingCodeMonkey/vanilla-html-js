# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a vanilla HTML/JavaScript project — no frameworks, no build tools, no package manager. The remote is `git@github.com:RovingCodeMonkey/vanilla-html-js.git`.

## Development

Open HTML files directly in a browser — there is no build step or dev server required.
